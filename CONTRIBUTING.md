# 贡献指南

感谢你对 Smart Gateway 的关注！

## 如何贡献

1. Fork 本仓库
2. 创建功能分支：`git checkout -b feature/my-feature`
3. 提交更改：`git commit -m 'feat: add my feature'`
4. 推送分支：`git push origin feature/my-feature`
5. 提交 Pull Request

## 提交规范

使用 Conventional Commits：
- `feat:` 新功能
- `fix:` 修复
- `docs:` 文档
- `refactor:` 重构
- `chore:` 杂务

## 开发环境

```bash
# 前端开发
cd web && npm install && npm run dev

# 后端开发
go mod tidy && go run .
```

## 问题反馈

请在 [Issues](https://github.com/sumo1111/smart-gateway/issues) 页面提交。
